import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
   appId: 'be.teomanliman.ionicproject',
   appName: 'WM-Z-Project',
   webDir: 'dist'
};

export default config;
